import React from 'react';
import NavComponent from '../NavComponent';

const Profile = () => {
  return (
    <div>
      <NavComponent />
      <h1>Perfil (Requiere inicio de sesión)</h1>
    </div>
  );
};

export default Profile;
